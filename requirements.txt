# Orchestration & LLM
langchain>=0.1.0
langchain-community>=0.0.10
langchain-ollama>=0.0.1
langgraph>=0.0.10

# SQL Processing
sqlglot>=20.0.0

# Database Drivers & ORM
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0    # Target DB (PostgreSQL) Driver

# Oracle drivers (choose one based on your environment)
# cx_Oracle>=8.3.0        # Requires Oracle Instant Client + C++ Build Tools
oracledb>=2.0.0           # Recommended: pure Python Oracle driver (thick/thin mode)

# Utilities
pyyaml>=6.0
pydantic>=2.0.0
tqdm>=4.65.0              # CLI progress bar

pymssql>=2.2.0           # MS SQL Server (or pyodbc)
sqlalchemy-hana>=0.5.0   # SAP HANA
snowflake-sqlalchemy>=1.4.0 # Snowflake
pymongo>=4.3.0           # MongoDB (NoSQL)
